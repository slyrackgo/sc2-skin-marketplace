{
  "name": "sc2-skin-marketplace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=contracts\"",
    "build": "npm run build --workspaces",
    "deploy": "npm run deploy --workspace=contracts",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}

